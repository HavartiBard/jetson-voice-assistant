# =============================================================================
# Jetson Voice Assistant - Environment Configuration
# =============================================================================
# Copy this file to .env and edit as needed.
# Settings in config/settings.json (via admin portal) take priority over these.
# =============================================================================

# -----------------------------------------------------------------------------
# OpenAI API (optional - for cloud LLM and Whisper API)
# Get your key at: https://platform.openai.com/
# -----------------------------------------------------------------------------
OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# Wake Word
# The word that activates the assistant (spoken before commands)
# -----------------------------------------------------------------------------
WAKE_WORD=jetson

# -----------------------------------------------------------------------------
# Whisper Speech-to-Text Settings
# -----------------------------------------------------------------------------
# Mode: "local" (faster-whisper, offline) or "api" (OpenAI Whisper, online)
WHISPER_MODE=local

# Model size for local mode: tiny, base, small, medium, large
# Smaller = faster but less accurate. "small" is a good balance for Jetson.
WHISPER_MODEL_SIZE=small

# Language code (e.g., en, es, fr, de, zh, ja)
WHISPER_LANGUAGE=en

# -----------------------------------------------------------------------------
# LLM Provider Settings
# -----------------------------------------------------------------------------
# Provider: "openai" (cloud) or "ollama" (local)
LLM_PROVIDER=openai

# Model name for the selected provider
# OpenAI: gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
# Ollama: llama3.2:1b, llama3.2:3b, phi3:mini, gemma2:2b, tinyllama, etc.
LLM_MODEL=gpt-4o-mini

# Ollama API endpoint (only used when LLM_PROVIDER=ollama)
OLLAMA_HOST=http://localhost:11434

# -----------------------------------------------------------------------------
# Audio Recording Settings
# -----------------------------------------------------------------------------
AUDIO_SAMPLE_RATE=16000
AUDIO_CHANNELS=1
AUDIO_RECORD_SECONDS=4

# -----------------------------------------------------------------------------
# Text-to-Speech Settings
# -----------------------------------------------------------------------------
# Provider: "gtts" (Google, natural voice, needs internet), 
#           "espeak" (robotic, offline), "pyttsx3" (system voices, offline)
TTS_PROVIDER=gtts

# Language code for TTS (e.g., en, es, fr, de)
TTS_LANGUAGE=en

# Speech rate for espeak/pyttsx3 (words per minute, 50-300)
TTS_SPEED=150

# -----------------------------------------------------------------------------
# Logging (for development/debugging)
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
